---
import { Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
const mediaUrl = import.meta.env.PUBLIC_MEDIA_URL;
---

<BaseLayout>
	<div class="hero-container">
		<Image 
			src={`${mediaUrl}/images/featured.jpg`} 
			alt="Featured image" 
			width={2400}
			height={1600}
			format="webp"
			class="hero-image"
		/>
	</div>
</BaseLayout>

<style>
	.hero-container {
		position: relative;
		height: calc(100vh - 60px - 2rem);
		margin: 0 -0.5rem; /* negative margin to offset parent padding */
		display: flex;
		justify-content: center;
		align-items: flex-start;
	}

	.hero-image {
		max-height: 100%;
		max-width: 100%;
		width: auto;
		height: auto;
		object-fit: contain;
		object-position: top;
	}
</style>
